/** TgFox Blue Console: legacy compact navigator retained for internal route type compatibility. */
export type AppView = "home" | "catalog" | "wallet" | "orders" | "profile";
export function BottomNav() { return null; }
